{% extends 'base.html.twig' %}

{% block title %}
    {{ parent() }}
    {#Produkt#}
{% endblock %}

{% block form %}

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary">Produkt ändern</button>
    {{ form_end(form) }}

{% endblock %}

{% block body %}
    {{ parent() }}
{% endblock %}
